package com.revature.application;

import com.revature.menus.StartMenu;

public class Application {

	public static void main(String[] args) {
		StartMenu.startMenu();
	}
	
	/**
	 * The requirements of this project are as follows:
	 * 
	 * Inherited from project 0.0
	 * - persistence
	 * - user creation/login
	 * - Admin
	 * - admins must approve users
	 * - admins can lock/unlock users
	 * - transactions
	 * - utilize logging & unit tests
	 * 
	 * New database related requirements
	 * - use JDBC for persistence
	 * - use a DAO
	 * - create three (3) stored procedures
	 * - create three (3) UDF
	 * - must only use PreparedStatement and CallableStatement
	 * - create two (2) sequences
	 * - create two (2) triggers
	 * - create custom checked exceptions where applicable
	 * - singleton
	 * - factory
	 * - tables must be in 3NF
	 * - provide a SetUp.sql file
	 * 
	 */

}
